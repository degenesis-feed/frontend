{
    "version": 2,
    "buildCommand": "npm install && npm run dev",
    "outputDirectory": "degenesis/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/degenesis/$1"
        }
    ]
}