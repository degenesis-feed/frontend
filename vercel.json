{
    "version": 2,
    "buildCommand": "cd degenesis && npm install && npm run build",
    "outputDirectory": "degenesis/.next",
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/degenesis/$1"
        }
    ]
}