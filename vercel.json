{
    "version": 2,
    "builds": [
        {
            "src": "degenesis/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "degenesis/.next"
            }
        }
    ]
}